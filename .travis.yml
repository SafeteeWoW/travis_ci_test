language: c
sudo: false

matrix:
  include:
    - os: linux
      env: EXTRA_TESTS=true
    - os: mac
      env: EXTRA_TESTS=true
  allow_failures:
    - os: linux
      env: EXTRA_TESTS=true
script:
  - python -V
  - false
